---
description: SentinelOne Singularity Data Lake (SDL) API — ingest events, run queries (PowerQuery, facet, timeseries, numeric), and manage configuration files (parsers, dashboards, alerts, lookups).
---

Read `sentinelone-sdl-api/SKILL.md` for full setup, workflow, authentication, and usage instructions. Reference docs are in `sentinelone-sdl-api/references/` and scripts are in `sentinelone-sdl-api/scripts/`.
